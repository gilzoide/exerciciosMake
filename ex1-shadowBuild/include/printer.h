#pragma once

/// Imprime um vetor na tela bacaninha, com um espaço entre cada e um \n no final
void printaVetor (int * v, int tamanho);

/// Imprime um vetor, só que ao contrário. Por que não?
void printaVetorAoContrario (int * v, int tamanho);
